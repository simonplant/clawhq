{
  "description": "Bugs and tech debt",
  "items": [
    {
      "id": "BUG-111",
      "title": "health poller marks instance active on SSH timeout fallback even when agent never started",
      "intent": "An instance must only be marked active when the agent is actually reachable. The SSH port timeout fallback that returns healthy:true after 18 attempts is incorrect — SSH up does not mean agent up.",
      "description": "In src/cloud/provisioning/health.ts lines 86-93, after 18 poll attempts (~3 minutes) where SSH is open but the dashboard port (3737) is not, pollInstanceHealth() returns healthy:true with the comment 'cloud-init may still be running but VM is functional'. This is wrong: SSH being up means the VM booted, not that the agent is running. With the clawhq up failure masking bug (BUG-109), this fallback causes the provisioning engine to mark instances active even when the agent never started. Even with BUG-109 fixed, the fallback allows a VM with a failed cloud-init to be reported as healthy. Fix: change the fallback to return healthy:false with a clear error — if port 3737 is not open after 3 minutes, the agent has not started.",
      "priority": "should",
      "category": null,
      "steps": [
        "Read src/cloud/provisioning/health.ts lines 52-105 to understand the current polling logic.",
        "At lines 86-93, change the fallback: instead of returning healthy:true, return healthy:false with error 'Agent did not become reachable on port 3737 within the expected window. SSH is accessible — connect with clawhq deploy update or check cloud-init logs: journalctl -u cloud-init'.",
        "Verify that the tests in health.test.ts (if they exist) cover the timeout path — if not, add a test for: SSH open + dashboard never opens + 19+ poll attempts = healthy:false with expected error message.",
        "Run npm test -- --testPathPattern=health to confirm all tests pass."
      ],
      "acceptanceCriteria": [
        "pollInstanceHealth() returns healthy:false when SSH is reachable but port 3737 never opens, regardless of number of attempts",
        "The error message in the result includes the SSH access hint for debugging",
        "Existing health tests pass with no regression"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomingNotes": "SPRINT AFTER BUG-109. The health poller's SSH-open fallback returns healthy:true after 18 attempts — this conflates 'VM booted' with 'agent running'. Even with BUG-109 fixed (failed cloud-init now exits non-zero), a VM whose cloud-init silently misbehaves in other ways could still trigger this path. The fix is conceptually simple: healthy:false on timeout with a clear message and SSH debug hint. FIX SCOPE: health.ts lines 86-93 + one new test case for the timeout path in health.test.ts. AC is correct and user-facing. Note: the error message wording in AC should match step 2 exactly — sprinter should use the journalctl hint verbatim.",
      "groomedAt": "2026-03-24",
      "lastFailReason": "Implementation failed",
      "lastFailAt": "2026-03-24T09:30:58-07:00",
      "failCount": 1
    },
    {
      "id": "BUG-113",
      "title": "SSH host key never collected at provision time — deploy update uses accept-new on every connection",
      "intent": "Every deploy update SSH connection must verify the remote host key against the key collected at provision time. accept-new must never be used for a known instance.",
      "description": "In engine.ts, addInstance() is called without sshHostKey. The ProvisionedInstance type has a sshHostKey field and the registry supports it, but nothing collects the host key after provisioning. As a result, all deploy update SSH connections hit the buildHostKeyArgs() fallback in update.ts (lines 224-231) which uses StrictHostKeyChecking=accept-new with UserKnownHostsFile=/dev/null. This means: (1) the host key is trusted blindly on first connect, (2) the key is never stored, (3) every subsequent deploy update re-trusts blindly again. A MITM attacker who intercepts any deploy update connection can inject commands into the remote agent. Fix: after provisioning health check passes, run ssh-keyscan -t ed25519 <ipAddress> to collect the host public key, then store it in the registry via a new updateInstanceField() call.",
      "priority": "should",
      "category": null,
      "steps": [],
      "acceptanceCriteria": [],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomingNotes": "SCOPE: engine.ts (after health check) + registry.ts (new helper to patch sshHostKey) + update.ts (no change needed — already reads sshHostKey from registry). STEPS: (1) In registry.ts, add updateInstanceSshHostKey(deployDir, instanceId, sshHostKey) — reads registry, patches the matching instance's sshHostKey field, writes atomically. (2) In engine.ts, after healthResult.healthy is true, run ssh-keyscan -t ed25519 <ipAddress> via execFile with a 10s timeout. Parse output to extract the key. On success, call updateInstanceSshHostKey(). On ssh-keyscan failure, log a warning but do not fail provisioning. (3) Write a test: provision succeeds → ssh-keyscan is called → sshHostKey is stored in registry. (4) Verify update.ts buildHostKeyArgs() strict path is exercised when sshHostKey is set (existing logic is correct). AC: (1) After successful provision, ProvisionedInstance.sshHostKey is set in instances.json. (2) A subsequent deploy update SSH call uses StrictHostKeyChecking=yes. (3) If ssh-keyscan fails at provision time, a warning is logged but provisioning still succeeds (sshHostKey remains unset). DEPENDENCY: none — sprint immediately. NOTE: update.ts already has the strict verification path — only the collection step is missing.",
      "groomedAt": "2026-03-24",
      "lastFailReason": "Implementation failed",
      "lastFailAt": "2026-03-24T09:31:10-07:00",
      "failCount": 1
    }
  ]
}
