#!/usr/bin/env python3
"""market-calendar — US market day/holiday detection and session timing.

Commands:
  market-calendar today        — is today a market day? what session?
  market-calendar status       — current market status (pre/open/post/closed)
  market-calendar next         — next market day
  market-calendar week         — this week's market days
  market-calendar check DATE   — is DATE a market day?

Add --json for machine-readable output.
"""
import os
import sys

MARKETS_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "markets")
sys.path.insert(0, MARKETS_DIR)

from market_calendar import main
main()
