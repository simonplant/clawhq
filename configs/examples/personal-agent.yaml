# Example non-interactive config for clawhq init --config
# Usage: clawhq init --config configs/examples/personal-agent.yaml
#
# Copy this file to your deployment directory and fill in real values.
# Never commit credentials to the clawhq repo.

blueprint: replace-my-pa
channel: telegram
model_provider: local
local_model: "llama3:8b"
deploy_dir: ~/.clawhq

user:
  name: User
  # timezone: America/New_York
  communication: brief

customization:
  work_style: "Professional but casual — friendly yet competent"
  scheduling_preference: "Suggest options — propose solutions, I confirm"
  priority_contacts: ""

# Auth — model provider credentials
# auth:
#   provider: anthropic
#   env:
#     ANTHROPIC_API_KEY: "sk-ant-..."
#   # Or for Claude subscription:
#   # env:
#   #   CLAUDE_AI_SESSION_KEY: "your-session-key"

# Channel credentials
# channels:
#   telegram:
#     bot_token: "123456:ABC-DEF..."

# Integrations — service credentials
# integrations:
#   messaging:
#     BOT_TOKEN: "your-telegram-bot-token"
#   email:
#     IMAP_HOST: "imap.gmail.com"
#     IMAP_USER: "you@gmail.com"
#     IMAP_PASS: "app-password"
#     SMTP_HOST: "smtp.gmail.com"
#     SMTP_USER: "you@gmail.com"
#     SMTP_PASS: "app-password"
#   calendar:
#     CALDAV_URL: "https://caldav.icloud.com"
#     CALDAV_USER: "you@icloud.com"
#     CALDAV_PASS: "app-password"
